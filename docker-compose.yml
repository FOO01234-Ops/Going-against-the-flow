version: "3.9"

services:

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  traffic-api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - PYTHONUNBUFFERED=1